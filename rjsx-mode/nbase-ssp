# -*- mode: snippet -*-
# name:nbase-ssp
# key:nbase-ssp
# description: pre-render this page on each request using the data returned by, only runs on server-side and never runs on the browser.
# --

export const getServerSideProps = async (context) => {
   return {
     props: {${1:Item}}
   }
}
